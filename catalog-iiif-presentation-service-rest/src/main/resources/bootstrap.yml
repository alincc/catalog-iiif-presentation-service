spring:
  application:
    name: catalog-iiif-presentation-service
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://localhost:8888}
      failFast: false

---

spring:
  profiles: docker
  cloud:
    config:
      failFast: true