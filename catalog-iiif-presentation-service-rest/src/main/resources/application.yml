info:
  app:
    name: catalog-iiif-presentation-service
    description: IIIF Presentation API

endpoints:
  restart:
    enabled: true
  refresh:
    enabled: true

server:
  port: 8050

microservice:
  contextUrl: http://iiif.io/api/presentation/2/context.json
  
---
spring:
    profiles: docker

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/
      
microservice:
  contextUrl: http://iiif.io/api/presentation/2/context.json